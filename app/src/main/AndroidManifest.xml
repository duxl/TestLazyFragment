<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a.b.c">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestLazyFragment">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".EmptyActivity" />

        <activity
            android:name=".AddShowHideActivity"
            android:label="测试生命周期/add、show、hide">
            <intent-filter>
                <action android:name="${action_value}" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ViewPagerActivity"
            android:label="测试生命周期/ViewPagerActivity">
            <intent-filter>
                <action android:name="${action_value}" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ManyFragmentActivity"
            android:label="解决多Fragment加载卡顿问题">
            <intent-filter>
                <action android:name="${action_value}" />
            </intent-filter>
        </activity>
    </application>

</manifest>